[project]
name = "data-downloader"
version = "0.1.0"
description = "Fetches OHLCV market data (via ccxt) into the Lean data folder."
requires-python = ">=3.11"
dependencies = [
    "ccxt==4.4.69",
]

[tool.uv]
package = false
