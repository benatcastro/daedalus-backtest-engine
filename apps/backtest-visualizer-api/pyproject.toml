[project]
name = "backtest-visualizer-api"
version = "0.1.0"
description = "Legacy backtest visualizer API and plotting tools (FastAPI + Plotly)."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.22.0",
    "pandas==2.2.3",
    "plotly==6.0.0",
    "PyYAML==6.0.2",
]

[tool.uv]
package = false
